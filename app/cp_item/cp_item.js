'use strict'

var createAll = function(ctx) {
    /**
     * TODO create capacity planning items during pi creation one for each team member
     */
}

var update = function(ctx) {
    /**
     * TODO update the capacity item of the team member when updated in Capacity planning UI
     */
}

var getAll = function(ctx) {
    /**
     * TODO retrieve all capacity items of the team members for a PI
     */
}

module.exports = {
    createAll,
    update,
    getAll
}